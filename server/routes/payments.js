'use strict';

// Placeholder — payment routes are handled via enrollment & webhooks
// This router exposes public payment-related status queries
const router = require('express').Router();

module.exports = router;
